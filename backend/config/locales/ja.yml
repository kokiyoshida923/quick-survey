ja:
  activerecord:
    models:
      user: ユーザー
    attributes:
      user:
        name: ユーザー名
        email: メールアドレス
        avatar: ユーザー画像
        password: パスワード
        password_confirmation: パスワード(確認用)
    errors:
      models:
        user:
          format: "%{attribute}%{message}"
  activemodel:
    attributes:
      session:
        email: メールアドレス
        password: パスワード
    errors:
      models:
        session:
          format: "%{attribute}%{message}"
